#ifndef LOG_H
#define LOG_H

// [НЕ ИСПОЛЬЗУЕТСЯ]

/*
#include <fstream>
#include <string>

class Logger {
    public:
    Logger(const std::string &logDirectory);
    void logMessage(const std::string &message);

    private:
    std::string logDirectory;
    void createLogDirectory();
    std::string getCurrentTime();
};

// Определение макроса для логирования
#define LOG(logger, message) logger.logMessage(message)
*/

#endif // LOG_H
